# Local agent workspace — Python 3.11+ recommended.
# Core orchestration uses only the Python standard library.
# No pip packages are required for the default flows.

# Optional (uncomment if you add YAML task files or tooling):
# pyyaml>=6.0
